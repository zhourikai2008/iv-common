import signalR from './signalR';
import webSocket from './webSocket';
import ImageResizer from './ImageResizer';
import {
  transformByte,
  transformDuration,
  transformObject,
  getJsonData,
} from './utils';


export default {
  signalR,
  webSocket,
  ImageResizer,

  transformByte,
  transformDuration,
  transformObject,
  getJsonData,
};
